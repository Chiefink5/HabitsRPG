const STORAGE_KEY="skillforge_upgraded_v1";
const ICON_SETS={All:["⛏","🪓","🗡","🛡","🧱","💎","🪙","⚒️","💪","💧","🧠","🏃","😴","🧘","❤️","🎸","🎵","🎤","🎨","✍️","💼","📚","🧾","🖥️","🔥","🍔","🧃","🍕","🍜","☕","🍫","🎮","🎬","🎲","🚀","👑","⭐","⚡","🌀","🔒","✅","🎯","📈","🏠","🧼","🧹","🌙","🥤","📦","💰"],Tools:["⛏","🪓","🗡","🛡","🧱","💎","🪙","⚒️","📦"],Health:["💪","💧","🧠","🏃","😴","🧘","❤️","🧼"],Music:["🎸","🎵","🎤","🎨","✍️"],Work:["💼","📚","🧾","🖥️","📈","🏠","🧹"],Food:["🍔","🧃","🍕","🍜","☕","🍫","🥤"],Fun:["🎮","🎬","🎲","🚀","👑","⭐","⚡","🌀"]};
const $=s=>document.querySelector(s), $$=s=>Array.from(document.querySelectorAll(s));
let currentIconTarget=null,audioCtx=null,rewardFilter="All",questFilter="Active",searchValue="",sortValue="recent";
const uid=p=>`${p}_${Math.random().toString(36).slice(2,10)}`,todayStr=()=>new Date().toISOString().slice(0,10),nowIso=()=>new Date().toISOString();
const xpRequiredForLevel=(b,l)=>Math.round(b*Math.pow(1.5,Math.max(0,l-1)));
function defaultState(){const g=uid("skill"),f=uid("skill"),l=uid("skill"),bh=uid("habit"),wh=uid("habit"),dh=uid("habit");return{meta:{lastOpenedDate:todayStr()},wallet:{coins:18,totalCoinsEarned:18,totalCoinsSpent:0},stats:{overallLevel:1,habitsCompletedToday:0,xpEarnedToday:0,coinsEarnedToday:0,rewardsRedeemed:0},skills:[{id:g,name:"Guitar",icon:"🎸",description:"Keep the fingers honest",level:1,xp:0,baseXp:50,xpRequired:50,expanded:true,createdAt:nowIso(),lastUsedAt:nowIso()},{id:f,name:"Fitness",icon:"💪",description:"Body grind",level:1,xp:0,baseXp:50,xpRequired:50,expanded:true,createdAt:nowIso(),lastUsedAt:nowIso()},{id:l,name:"Learning",icon:"🧠",description:"Level the brain",level:1,xp:0,baseXp:50,xpRequired:50,expanded:false,createdAt:nowIso(),lastUsedAt:nowIso()}],habits:[{id:bh,skillId:g,name:"Practice Bends",icon:"🔥",description:"Accuracy and pitch",xpReward:15,coinReward:3,dailyCap:3,timesCompletedToday:0,totalCompletions:0,streak:0,bestStreak:0,lastCompletedDate:null,streakEnabled:true,isNegative:false,createdAt:nowIso()},{id:uid("habit"),skillId:g,name:"Warmups",icon:"🎯",description:"Loosen up",xpReward:10,coinReward:2,dailyCap:2,timesCompletedToday:0,totalCompletions:0,streak:0,bestStreak:0,lastCompletedDate:null,streakEnabled:true,isNegative:false,createdAt:nowIso()},{id:wh,skillId:f,name:"Drink Water",icon:"💧",description:"Hydrate",xpReward:8,coinReward:1,dailyCap:5,timesCompletedToday:0,totalCompletions:0,streak:0,bestStreak:0,lastCompletedDate:null,streakEnabled:true,isNegative:false,createdAt:nowIso()},{id:uid("habit"),skillId:l,name:"Read 10 Pages",icon:"📚",description:"Just read the damn thing",xpReward:12,coinReward:2,dailyCap:2,timesCompletedToday:0,totalCompletions:0,streak:0,bestStreak:0,lastCompletedDate:null,streakEnabled:true,isNegative:false,createdAt:nowIso()},{id:dh,skillId:l,name:"Doom Scroll",icon:"🌀",description:"Negative habit example",xpReward:8,coinReward:1,dailyCap:4,timesCompletedToday:0,totalCompletions:0,streak:0,bestStreak:0,lastCompletedDate:null,streakEnabled:true,isNegative:true,createdAt:nowIso()}],rewards:[{id:uid("reward"),name:"1 Hour Gaming",icon:"🎮",category:"Fun",cost:30,repeatable:true,note:"No guilt run",createdAt:nowIso()},{id:uid("reward"),name:"Energy Drink",icon:"🧃",category:"Food",cost:15,repeatable:true,note:"One can only",createdAt:nowIso()},{id:uid("reward"),name:"Movie Night",icon:"🎬",category:"Rest",cost:45,repeatable:true,note:"Kick back",createdAt:nowIso()}],quests:[{id:uid("quest"),type:"habit_count",title:"Complete Drink Water 7 times",targetId:wh,targetValue:7,rewardCoins:20,completed:false,completedAt:null,createdAt:nowIso()},{id:uid("quest"),type:"skill_level",title:"Reach Guitar level 2",targetId:g,targetValue:2,rewardCoins:25,completed:false,completedAt:null,createdAt:nowIso()}],activityLog:[]}}
let state=loadState();
function loadState(){try{const raw=localStorage.getItem(STORAGE_KEY);const parsed=raw?JSON.parse(raw):defaultState();ensureDataShape(parsed);handleDailyReset(parsed);return parsed}catch(e){return defaultState()}}
function ensureDataShape(d){d.meta||={lastOpenedDate:todayStr()};d.wallet||={coins:0,totalCoinsEarned:0,totalCoinsSpent:0};d.stats||={overallLevel:1,habitsCompletedToday:0,xpEarnedToday:0,coinsEarnedToday:0,rewardsRedeemed:0};d.skills||=[];d.habits||=[];d.rewards||=[];d.quests||=[];d.activityLog||=[];d.skills.forEach(s=>{s.baseXp||=50;s.level||=1;s.xp||=0;s.xpRequired||=xpRequiredForLevel(s.baseXp,s.level);if(s.expanded===undefined)s.expanded=false;s.lastUsedAt||=s.createdAt||nowIso()});d.habits.forEach(h=>{h.description||="";h.timesCompletedToday||=0;h.totalCompletions||=0;h.streak||=0;h.bestStreak||=0;if(h.streakEnabled===undefined)h.streakEnabled=true;if(h.isNegative===undefined)h.isNegative=false})}
function handleDailyReset(d){const t=todayStr();if(d.meta.lastOpenedDate!==t){d.habits.forEach(h=>h.timesCompletedToday=0);d.stats.habitsCompletedToday=0;d.stats.xpEarnedToday=0;d.stats.coinsEarnedToday=0;d.meta.lastOpenedDate=t;saveState()}}
function saveState(){localStorage.setItem(STORAGE_KEY,JSON.stringify(state))}
function addLog(text){state.activityLog.unshift({id:uid("log"),text,createdAt:nowIso()});state.activityLog=state.activityLog.slice(0,20)}
const getSkill=id=>state.skills.find(s=>s.id===id), getHabit=id=>state.habits.find(h=>h.id===id);
function recomputeOverallLevel(){const total=state.skills.reduce((sum,s)=>sum+(s.level||1),0);state.stats.overallLevel=Math.max(1,Math.floor(total/Math.max(1,state.skills.length))||1)}
const rankLabel=l=>l>=10?"Legend":l>=7?"Overboss":l>=5?"Veteran":l>=3?"Runner":"Fresh Spawn";
const characterEmoji=l=>l>=10?"🦾":l>=7?"🧙":l>=5?"⚔️":l>=3?"🧢":"🙂";
function showToast(message,kind="good"){const wrap=$("#toastStack"),el=document.createElement("div");el.className=`toast ${kind}`;el.innerHTML=message;wrap.appendChild(el);setTimeout(()=>{el.style.opacity="0";el.style.transform="translateY(8px)";setTimeout(()=>el.remove(),180)},2400)}
function spawnFloatingText(targetEl,text,kind="good"){if(!targetEl)return;const rect=targetEl.getBoundingClientRect(),float=document.createElement("div");float.className=`floating-text floating-${kind}`;float.textContent=text;float.style.left=`${rect.left+rect.width/2}px`;float.style.top=`${rect.top-4}px`;$("#floatLayer").appendChild(float);setTimeout(()=>float.remove(),920)}
function playTone(freq=440,duration=.08,type="sine",volume=.03){try{audioCtx||=new(window.AudioContext||window.webkitAudioContext)();const osc=audioCtx.createOscillator(),gain=audioCtx.createGain();osc.frequency.value=freq;osc.type=type;gain.gain.value=volume;osc.connect(gain);gain.connect(audioCtx.destination);osc.start();gain.gain.exponentialRampToValueAtTime(.0001,audioCtx.currentTime+duration);osc.stop(audioCtx.currentTime+duration)}catch(e){}}
const playLevelUpSound=()=>[440,660,880].forEach((f,i)=>setTimeout(()=>playTone(f,.12,"triangle",.04),i*90)),playClickGood=()=>playTone(560,.06,"square",.02),playClickBad=()=>playTone(220,.08,"sawtooth",.018),playQuestSound=()=>[530,700].forEach((f,i)=>setTimeout(()=>playTone(f,.09,"triangle",.035),i*70));
function openModal(id){$("#modalBackdrop").classList.remove("hidden");$(`#${id}`).classList.remove("hidden")}function closeAllModals(){$("#modalBackdrop").classList.add("hidden");$$(".modal").forEach(m=>m.classList.add("hidden"))}
function setActiveTab(tab){$$(".tab-btn").forEach(btn=>btn.classList.toggle("active",btn.dataset.tab===tab));$$(".tab-panel").forEach(p=>p.classList.remove("active"));$(`#${tab}Tab`).classList.add("active");if(tab==="rewards")$("#quickAddMenu").classList.add("hidden")}
function render(){saveState();recomputeOverallLevel();$("#overallLevelBtn").textContent=`Lv ${state.stats.overallLevel}`;$("#walletBtn").textContent=`🪙 ${state.wallet.coins}`;renderDashboard();renderSkills();renderRewards();renderQuests();populateSelects()}
function renderDashboard(){const active=state.quests.filter(q=>!q.completed).length,top=[...state.skills].sort((a,b)=>(b.level-a.level)||(b.xp-a.xp)).slice(0,3),rwd=state.rewards.slice(0,3),qp=state.quests.filter(q=>!q.completed).slice(0,3),recent=state.activityLog.slice(0,5);$("#dashboardTab").innerHTML=`<div class="panel hero-card"><div class="character-box"><div><div class="mini-character">${characterEmoji(state.stats.overallLevel)}</div><div class="character-rank">${rankLabel(state.stats.overallLevel)}</div></div></div><div><div class="panel-header"><div><h3>Character Panel</h3><div class="muted">Tiny dashboard avatar + grind stats</div></div><button class="secondary-btn" data-open-modal="habitModal">+ Habit</button></div><div class="hero-stats"><div class="stat-card"><div class="stat-value">${state.stats.overallLevel}</div><div class="stat-label">Overall Level</div></div><div class="stat-card"><div class="stat-value">${state.wallet.coins}</div><div class="stat-label">Coins</div></div><div class="stat-card"><div class="stat-value">${state.stats.habitsCompletedToday}</div><div class="stat-label">Habits Today</div></div><div class="stat-card"><div class="stat-value">${active}</div><div class="stat-label">Active Quests</div></div><div class="stat-card"><div class="stat-value">${state.stats.xpEarnedToday}</div><div class="stat-label">XP Today</div></div><div class="stat-card"><div class="stat-value">${state.stats.rewardsRedeemed}</div><div class="stat-label">Rewards Redeemed</div></div></div></div></div><div class="section-spacer"></div><div class="grid-2"><div class="panel"><div class="panel-header"><h3>Top Skills</h3><button class="secondary-btn" data-go-tab="skills">Open Skills</button></div><div class="list-stack">${top.length?top.map(s=>`<div class="mini-row"><div><strong>${s.icon} ${escapeHtml(s.name)}</strong><div class="muted">Lv ${s.level}</div></div><button class="ghost-btn" data-skill-focus="${s.id}">View</button></div>`).join(""):`<div class="empty-state">No skills yet.</div>`}</div></div><div class="panel"><div class="panel-header"><h3>Daily Snapshot</h3><button class="secondary-btn" data-open-modal="questModal">+ Quest</button></div><div class="list-stack"><div class="mini-row"><span>XP earned</span><strong>${state.stats.xpEarnedToday}</strong></div><div class="mini-row"><span>Coins earned</span><strong>${state.stats.coinsEarnedToday}</strong></div><div class="mini-row"><span>Skills total</span><strong>${state.skills.length}</strong></div><div class="mini-row"><span>Rewards in shop</span><strong>${state.rewards.length}</strong></div></div></div></div><div class="section-spacer"></div><div class="grid-2"><div class="panel"><div class="panel-header"><h3>Quest Preview</h3><button class="secondary-btn" data-go-tab="quests">View All</button></div><div class="list-stack">${qp.length?qp.map(q=>{const p=getQuestProgress(q);return`<div class="mini-row"><div><strong>${questIcon(q)} ${q.title}</strong><div class="muted">${p.current}/${p.target}</div></div><div class="reward-price">🪙 ${q.rewardCoins}</div></div>`}).join(""):`<div class="empty-state">No active quests.</div>`}</div></div><div class="panel"><div class="panel-header"><h3>Reward Preview</h3><button class="secondary-btn" data-go-tab="rewards">Open Shop</button></div><div class="list-stack">${rwd.length?rwd.map(r=>`<div class="mini-row"><div><strong>${r.icon} ${escapeHtml(r.name)}</strong><div class="muted">${r.category}</div></div><div class="reward-price">🪙 ${r.cost}</div></div>`).join(""):`<div class="empty-state">No rewards set up.</div>`}</div></div></div><div class="section-spacer"></div><div class="panel"><div class="panel-header"><h3>Recent Activity</h3></div><div class="list-stack">${recent.length?recent.map(log=>`<div class="mini-row"><span>${log.text}</span></div>`).join(""):`<div class="empty-state">Do something. Then the feed wakes up.</div>`}</div></div>`}
function renderSkills(){const filtered=[...state.skills].sort(skillSorter).filter(skill=>`${skill.name} ${skill.description} ${state.habits.filter(h=>h.skillId===skill.id).map(h=>`${h.name} ${h.description}`).join(" ")}`.toLowerCase().includes(searchValue.toLowerCase()));$("#skillsTab").innerHTML=`<div class="panel"><div class="panel-header"><div><h3>Skills</h3><div class="muted">Expandable skill trees, positive and negative habits</div></div><button class="primary-btn" data-open-modal="skillModal">+ Skill</button></div><div class="search-row"><input id="skillSearchInput" placeholder="Search skills or habits..." value="${escapeHtml(searchValue)}" /><select id="skillSortSelect"><option value="recent" ${sortValue==="recent"?"selected":""}>Sort: Recent</option><option value="level" ${sortValue==="level"?"selected":""}>Sort: Level</option><option value="name" ${sortValue==="name"?"selected":""}>Sort: Name</option><option value="progress" ${sortValue==="progress"?"selected":""}>Sort: Progress</option></select></div>${filtered.length?filtered.map(renderSkillCard).join(""):`<div class="empty-state">No matching skills. Add one or clear the filter.</div>`}</div>`}
function renderSkillCard(skill){const habits=state.habits.filter(h=>h.skillId===skill.id),pct=Math.min(100,Math.round(skill.xp/skill.xpRequired*100));return`<div class="skill-card" id="skill-${skill.id}"><div class="skill-head"><div class="skill-title-wrap"><div class="skill-icon">${skill.icon}</div><div><h4>${escapeHtml(skill.name)}</h4><div class="muted">${escapeHtml(skill.description||"No description")}</div></div></div><div class="inline-actions"><span class="level-pill">Lv ${skill.level}</span><button class="edit-icon-btn" data-edit-skill="${skill.id}">✏️</button><button class="edit-icon-btn" data-toggle-skill="${skill.id}">${skill.expanded?"▾":"▸"}</button></div></div><div class="progress" style="margin-top:12px"><div class="progress-inner" style="width:${pct}%"></div></div><div class="progress-label"><span>${skill.xp}/${skill.xpRequired} XP</span><span>${habits.length} habits</span></div><div class="skill-actions" style="margin-top:12px"><button class="secondary-btn" data-add-habit-under="${skill.id}">+ Habit</button><button class="ghost-btn" data-toggle-skill="${skill.id}">${skill.expanded?"Collapse Tree":"Open Tree"}</button></div>${skill.expanded?`<div class="tree">${habits.length?habits.map(h=>renderHabitCard(h)).join(""):`<div class="empty-state">No habits in this skill yet. Add one.</div>`}</div>`:""}</div>`}
function renderHabitCard(h){const skill=getSkill(h.skillId),streak=h.streakEnabled?h.streak:"off";return`<div class="habit-card"><div class="habit-top"><div class="habit-title"><div class="skill-icon" style="width:40px;height:40px;font-size:20px">${h.icon}</div><div><strong>${escapeHtml(h.name)}</strong><div class="muted">${escapeHtml(h.description||"No description")}</div></div></div><div class="item-actions"><button class="edit-icon-btn" data-edit-habit="${h.id}">✏️</button></div></div><div class="habit-meta"><span class="tag">${h.isNegative?`-${Math.ceil(h.xpReward*1.5)} XP`:`+${h.xpReward} XP`}</span><span class="tag">${h.isNegative?`-${Math.ceil(h.coinReward*1.5)} Coins`:`+${h.coinReward} Coins`}</span><span class="tag">Today ${h.timesCompletedToday}/${h.dailyCap}</span><span class="tag">Streak ${streak}</span>${h.isNegative?`<span class="tag tag-negative">Negative Habit</span>`:""}</div><div class="habit-bottom"><div class="muted">${skill?skill.icon+" "+escapeHtml(skill.name):""}</div><div class="action-cluster"><button class="minus-btn" data-undo-habit="${h.id}" title="Undo one">−</button><button class="plus-btn" data-hit-habit="${h.id}" title="Apply">${h.isNegative?"!":"+"}</button></div></div></div>`}
function renderRewards(){const filtered=state.rewards.filter(r=>rewardFilter==="All"||r.category===rewardFilter),cats=["All",...new Set(state.rewards.map(r=>r.category))];$("#rewardsTab").innerHTML=`<div class="panel"><div class="panel-header"><div><h3>Reward Shop</h3><div class="muted">Inventory-style store panel</div></div><button class="primary-btn" data-open-modal="rewardModal">+ Reward</button></div><div class="mini-row" style="margin-bottom:12px"><div><strong>Wallet</strong><div class="muted">Spend coins on mini wins</div></div><div class="reward-price">🪙 ${state.wallet.coins}</div></div><div class="filters">${cats.map(cat=>`<button class="filter-pill ${rewardFilter===cat?"active":""}" data-reward-filter="${cat}">${cat}</button>`).join("")}</div>${filtered.length?`<div class="inventory-grid">${filtered.map(renderRewardCard).join("")}</div>`:`<div class="empty-state">No rewards in this filter.</div>`}</div>`}
function renderRewardCard(r){const aff=state.wallet.coins>=r.cost;return`<div class="reward-card ${!aff?"reward-disabled":""}"><div class="reward-icon-box">${r.icon}</div><strong>${escapeHtml(r.name)}</strong><div class="muted" style="margin-top:4px">${escapeHtml(r.category)}</div><div class="muted" style="margin-top:4px; min-height:36px">${escapeHtml(r.note||"No note")}</div><hr class="soft" /><div class="panel-header" style="margin-bottom:0"><span class="reward-price">🪙 ${r.cost}</span><span class="tag">${r.repeatable?"Repeatable":"One-time"}</span></div><div class="item-actions" style="margin-top:10px"><button class="primary-btn" ${aff?"":"disabled"} data-buy-reward="${r.id}">Redeem</button><button class="secondary-btn" data-edit-reward="${r.id}">Edit</button></div></div>`}
function renderQuests(){const quests=state.quests.filter(q=>questFilter==="All"?true:questFilter==="Completed"?q.completed:!q.completed);$("#questsTab").innerHTML=`<div class="panel"><div class="panel-header"><div><h3>Quests</h3><div class="muted">Auto-complete and auto-pay when done</div></div><button class="primary-btn" data-open-modal="questModal">+ Quest</button></div><div class="filters">${["Active","Completed","All"].map(type=>`<button class="filter-pill ${questFilter===type?"active":""}" data-quest-filter="${type}">${type}</button>`).join("")}</div><div class="list-stack">${quests.length?quests.map(renderQuestCard).join(""):`<div class="empty-state">No quests in this view.</div>`}</div></div>`}
function renderQuestCard(q){const p=getQuestProgress(q),pct=Math.min(100,Math.round(p.current/p.target*100));return`<div class="quest-card ${q.completed?"quest-complete":""}"><div class="panel-header"><div><strong>${questIcon(q)} ${escapeHtml(q.title)}</strong><div class="muted">${questTypeLabel(q.type)}</div></div><div class="item-actions"><button class="secondary-btn" data-edit-quest="${q.id}">Edit</button></div></div><div class="progress"><div class="progress-inner" style="width:${pct}%"></div></div><div class="quest-progress"><span>${p.current}/${p.target}</span><span>Reward 🪙 ${q.rewardCoins}</span></div>${q.completed?`<div class="tiny-note" style="margin-top:8px">Completed ${formatDate(q.completedAt)}</div>`:""}</div>`}
const questTypeLabel=t=>({habit_count:"Habit Count",skill_level:"Skill Level",streak_goal:"Streak Goal",coin_goal:"Coin Goal"})[t]||t, questIcon=q=>({habit_count:"📜",skill_level:"⭐",streak_goal:"🔥",coin_goal:"🪙"})[q.type]||"📜";
function getQuestProgress(q){if(q.type==="habit_count"){const h=getHabit(q.targetId);return{current:h?h.totalCompletions:0,target:q.targetValue}}if(q.type==="skill_level"){const s=getSkill(q.targetId);return{current:s?s.level:0,target:q.targetValue}}if(q.type==="streak_goal"){const h=getHabit(q.targetId);return{current:h?h.streak:0,target:q.targetValue}}if(q.type==="coin_goal")return{current:state.wallet.totalCoinsEarned,target:q.targetValue};return{current:0,target:q.targetValue||1}}
function checkQuestCompletion(){let any=false;state.quests.forEach(q=>{if(q.completed)return;const p=getQuestProgress(q);if(p.current>=p.target){q.completed=true;q.completedAt=nowIso();state.wallet.coins+=q.rewardCoins;state.wallet.totalCoinsEarned+=q.rewardCoins;addLog(`Quest complete — ${q.title} (+${q.rewardCoins} coins)`);showToast(`Quest complete<br><strong>${escapeHtml(q.title)}</strong><br>+${q.rewardCoins} coins`,"good");playQuestSound();any=true}});if(any)saveState()}
function applyHabit(id,btn){const h=getHabit(id);if(!h)return;const s=getSkill(h.skillId);if(!s)return;if(h.timesCompletedToday>=h.dailyCap){showToast(`Cap reached for <strong>${escapeHtml(h.name)}</strong>`,"bad");playClickBad();spawnFloatingText(btn,"CAP","bad");return}const neg=h.isNegative,xp=neg?Math.ceil(h.xpReward*1.5):h.xpReward,coin=neg?Math.ceil(h.coinReward*1.5):h.coinReward;h.timesCompletedToday++;h.totalCompletions++;const last=h.lastCompletedDate;if(h.streakEnabled){if(last!==todayStr()){if(last){const diff=Math.floor((new Date(todayStr())-new Date(last))/(1000*60*60*24));h.streak=diff===1?h.streak+1:1}else h.streak=1;h.bestStreak=Math.max(h.bestStreak,h.streak)}}h.lastCompletedDate=todayStr();s.lastUsedAt=nowIso();if(neg){s.xp=Math.max(0,s.xp-xp);state.wallet.coins=Math.max(0,state.wallet.coins-coin);addLog(`Negative habit — ${h.name} (-${xp} XP / -${coin} coins)`);showToast(`Negative habit hit<br><strong>${escapeHtml(h.name)}</strong><br>-${xp} XP / -${coin} coins`,"bad");playClickBad();spawnFloatingText(btn,`-${xp} XP`,"bad")}else{s.xp+=xp;state.wallet.coins+=coin;state.wallet.totalCoinsEarned+=coin;state.stats.habitsCompletedToday++;state.stats.xpEarnedToday+=xp;state.stats.coinsEarnedToday+=coin;addLog(`+${xp} XP / +${coin} coins — ${h.name}`);showToast(`+${xp} XP / +${coin} coins<br><strong>${escapeHtml(h.name)}</strong>`,"good");playClickGood();spawnFloatingText(btn,`+${xp} XP`,"good");spawnFloatingText(btn,`+${coin} 🪙`,"gold")}while(s.xp>=s.xpRequired){s.xp-=s.xpRequired;s.level++;s.xpRequired=xpRequiredForLevel(s.baseXp,s.level);addLog(`Level up — ${s.name} Lv ${s.level}`);showToast(`Level up!<br><strong>${escapeHtml(s.name)}</strong> is now Lv ${s.level}`,"good");playLevelUpSound()}recomputeOverallLevel();checkQuestCompletion();saveState();render()}
function undoHabit(id,btn){const h=getHabit(id);if(!h)return;const s=getSkill(h.skillId);if(!s)return;if(h.timesCompletedToday<=0){showToast(`Nothing to undo for <strong>${escapeHtml(h.name)}</strong>`,"bad");return}const neg=h.isNegative,xp=neg?Math.ceil(h.xpReward*1.5):h.xpReward,coin=neg?Math.ceil(h.coinReward*1.5):h.coinReward;h.timesCompletedToday=Math.max(0,h.timesCompletedToday-1);h.totalCompletions=Math.max(0,h.totalCompletions-1);if(neg){s.xp+=xp;state.wallet.coins+=coin;addLog(`Undo negative habit — ${h.name}`);spawnFloatingText(btn,`+${xp} XP`,"good")}else{s.xp=Math.max(0,s.xp-xp);state.wallet.coins=Math.max(0,state.wallet.coins-coin);state.stats.habitsCompletedToday=Math.max(0,state.stats.habitsCompletedToday-1);state.stats.xpEarnedToday=Math.max(0,state.stats.xpEarnedToday-xp);state.stats.coinsEarnedToday=Math.max(0,state.stats.coinsEarnedToday-coin);addLog(`Undo habit — ${h.name}`);spawnFloatingText(btn,`-${xp} XP`,"bad")}playClickGood();saveState();render()}
function buyReward(id){const r=state.rewards.find(x=>x.id===id);if(!r)return;if(state.wallet.coins<r.cost){showToast(`Need ${r.cost-state.wallet.coins} more coins for <strong>${escapeHtml(r.name)}</strong>`,"bad");playClickBad();return}state.wallet.coins-=r.cost;state.wallet.totalCoinsSpent+=r.cost;state.stats.rewardsRedeemed++;addLog(`Redeemed reward — ${r.name} (-${r.cost} coins)`);showToast(`Redeemed<br><strong>${escapeHtml(r.name)}</strong><br>-${r.cost} coins`,"good");playQuestSound();if(!r.repeatable)state.rewards=state.rewards.filter(x=>x.id!==id);saveState();render()}
function populateSelects(){const skillOpts=state.skills.map(s=>`<option value="${s.id}">${escapeHtml(s.icon)} ${escapeHtml(s.name)}</option>`).join("");$("#habitSkillId").innerHTML=skillOpts||"";$("#questTargetSkill").innerHTML=skillOpts||"";const habitOpts=state.habits.map(h=>`<option value="${h.id}">${escapeHtml(h.icon)} ${escapeHtml(h.name)}</option>`).join("");$("#questTargetHabit").innerHTML=habitOpts||""}
function openSkillModal(id=null){const e=!!id,s=e?getSkill(id):null;$("#skillModalTitle").textContent=e?"Edit Skill":"Add Skill";$("#skillId").value=s?.id||"";$("#skillName").value=s?.name||"";$("#skillBaseXp").value=s?.baseXp||50;$("#skillDescription").value=s?.description||"";$("#skillIcon").value=s?.icon||"🎸";$("#skillIconPreview").textContent=s?.icon||"🎸";$("#deleteSkillBtn").classList.toggle("hidden",!e);openModal("skillModal")}
function openHabitModal(id=null,skillPrefill=null){populateSelects();const e=!!id,h=e?getHabit(id):null;$("#habitModalTitle").textContent=e?"Edit Habit":"Add Habit";$("#habitId").value=h?.id||"";$("#habitName").value=h?.name||"";$("#habitSkillId").value=h?.skillId||skillPrefill||state.skills[0]?.id||"";$("#habitXpReward").value=h?.xpReward||10;$("#habitCoinReward").value=h?.coinReward||2;$("#habitDailyCap").value=h?.dailyCap||1;$("#habitDescription").value=h?.description||"";$("#habitNegative").checked=!!h?.isNegative;$("#habitIcon").value=h?.icon||"🔥";$("#habitIconPreview").textContent=h?.icon||"🔥";$("#deleteHabitBtn").classList.toggle("hidden",!e);openModal("habitModal")}
function openRewardModal(id=null){const e=!!id,r=e?state.rewards.find(x=>x.id===id):null;$("#rewardModalTitle").textContent=e?"Edit Reward":"Add Reward";$("#rewardId").value=r?.id||"";$("#rewardName").value=r?.name||"";$("#rewardCost").value=r?.cost||25;$("#rewardCategory").value=r?.category||"Fun";$("#rewardNote").value=r?.note||"";$("#rewardRepeatable").checked=r?.repeatable!==false;$("#rewardIcon").value=r?.icon||"🎮";$("#rewardIconPreview").textContent=r?.icon||"🎮";$("#deleteRewardBtn").classList.toggle("hidden",!e);openModal("rewardModal")}
function openQuestModal(id=null){populateSelects();const e=!!id,q=e?state.quests.find(x=>x.id===id):null;$("#questModalTitle").textContent=e?"Edit Quest":"Add Quest";$("#questId").value=q?.id||"";$("#questType").value=q?.type||"habit_count";$("#questTargetHabit").value=q&&(q.type==="habit_count"||q.type==="streak_goal")?q.targetId:state.habits[0]?.id||"";$("#questTargetSkill").value=q&&q.type==="skill_level"?q.targetId:state.skills[0]?.id||"";$("#questTargetValue").value=q?.targetValue||5;$("#questRewardCoins").value=q?.rewardCoins||20;$("#questTitle").value=q?.title||"";$("#deleteQuestBtn").classList.toggle("hidden",!e);updateQuestTargetVisibility();openModal("questModal")}
function updateQuestTargetVisibility(){const type=$("#questType").value;$("#questTargetHabitWrap").classList.toggle("hidden",!(type==="habit_count"||type==="streak_goal"));$("#questTargetSkillWrap").classList.toggle("hidden",type!=="skill_level")}
function autoQuestTitle(type,targetId,targetValue){if(type==="habit_count"){const h=getHabit(targetId);return`Complete ${h?.name||"habit"} ${targetValue} times`}if(type==="skill_level"){const s=getSkill(targetId);return`Reach ${s?.name||"skill"} level ${targetValue}`}if(type==="streak_goal"){const h=getHabit(targetId);return`Maintain ${h?.name||"habit"} streak for ${targetValue} days`}return`Earn ${targetValue} coins`}
function submitSkillForm(e){e.preventDefault();const id=$("#skillId").value,p={id:id||uid("skill"),name:$("#skillName").value.trim(),baseXp:Number($("#skillBaseXp").value),description:$("#skillDescription").value.trim(),icon:$("#skillIcon").value,level:1,xp:0,xpRequired:Number($("#skillBaseXp").value),expanded:true,createdAt:nowIso(),lastUsedAt:nowIso()};if(!p.name)return;if(id){const x=getSkill(id);x.name=p.name;x.baseXp=p.baseXp;x.description=p.description;x.icon=p.icon;x.xpRequired=xpRequiredForLevel(x.baseXp,x.level);addLog(`Updated skill — ${x.name}`)}else{state.skills.push(p);addLog(`Added skill — ${p.name}`)}saveState();closeAllModals();render()}
function submitHabitForm(e){e.preventDefault();const id=$("#habitId").value,p={id:id||uid("habit"),skillId:$("#habitSkillId").value,name:$("#habitName").value.trim(),xpReward:Number($("#habitXpReward").value),coinReward:Number($("#habitCoinReward").value),dailyCap:Number($("#habitDailyCap").value),description:$("#habitDescription").value.trim(),icon:$("#habitIcon").value,streakEnabled:true,isNegative:$("#habitNegative").checked,createdAt:nowIso(),timesCompletedToday:0,totalCompletions:0,streak:0,bestStreak:0,lastCompletedDate:null};if(!p.name||!p.skillId)return;if(id){const x=getHabit(id);x.skillId=p.skillId;x.name=p.name;x.xpReward=p.xpReward;x.coinReward=p.coinReward;x.dailyCap=p.dailyCap;x.description=p.description;x.icon=p.icon;x.isNegative=p.isNegative;addLog(`Updated habit — ${x.name}`)}else{state.habits.push(p);addLog(`Added habit — ${p.name}`)}saveState();closeAllModals();render()}
function submitRewardForm(e){e.preventDefault();const id=$("#rewardId").value,p={id:id||uid("reward"),name:$("#rewardName").value.trim(),cost:Number($("#rewardCost").value),category:$("#rewardCategory").value,note:$("#rewardNote").value.trim(),repeatable:$("#rewardRepeatable").checked,icon:$("#rewardIcon").value,createdAt:nowIso()};if(!p.name)return;if(id){Object.assign(state.rewards.find(r=>r.id===id),p);addLog(`Updated reward — ${p.name}`)}else{state.rewards.push(p);addLog(`Added reward — ${p.name}`)}saveState();closeAllModals();render()}
function submitQuestForm(e){e.preventDefault();const id=$("#questId").value,type=$("#questType").value,targetId=type==="skill_level"?$("#questTargetSkill").value:type==="coin_goal"?null:$("#questTargetHabit").value,targetValue=Number($("#questTargetValue").value),title=$("#questTitle").value.trim()||autoQuestTitle(type,targetId,targetValue),p={id:id||uid("quest"),type,title,targetId,targetValue,rewardCoins:Number($("#questRewardCoins").value),completed:false,completedAt:null,createdAt:nowIso()};if(id){const x=state.quests.find(q=>q.id===id);x.type=p.type;x.title=p.title;x.targetId=p.targetId;x.targetValue=p.targetValue;x.rewardCoins=p.rewardCoins;x.completed=false;x.completedAt=null;addLog(`Updated quest — ${x.title}`)}else{state.quests.push(p);addLog(`Added quest — ${p.title}`)}checkQuestCompletion();saveState();closeAllModals();render()}
function deleteSkill(){const id=$("#skillId").value,skill=getSkill(id);if(!skill||!confirm(`Delete skill "${skill.name}" and all its habits?`))return;state.skills=state.skills.filter(s=>s.id!==id);state.habits=state.habits.filter(h=>h.skillId!==id);state.quests=state.quests.filter(q=>q.targetId!==id);addLog(`Deleted skill — ${skill.name}`);closeAllModals();saveState();render()}
function deleteHabit(){const id=$("#habitId").value,h=getHabit(id);if(!h||!confirm(`Delete habit "${h.name}"?`))return;state.habits=state.habits.filter(x=>x.id!==id);state.quests=state.quests.filter(q=>q.targetId!==id);addLog(`Deleted habit — ${h.name}`);closeAllModals();saveState();render()}
function deleteReward(){const id=$("#rewardId").value,r=state.rewards.find(x=>x.id===id);if(!r||!confirm(`Delete reward "${r.name}"?`))return;state.rewards=state.rewards.filter(x=>x.id!==id);addLog(`Deleted reward — ${r.name}`);closeAllModals();saveState();render()}
function deleteQuest(){const id=$("#questId").value,q=state.quests.find(x=>x.id===id);if(!q||!confirm(`Delete quest "${q.title}"?`))return;state.quests=state.quests.filter(x=>x.id!==id);addLog(`Deleted quest — ${q.title}`);closeAllModals();saveState();render()}
function skillSorter(a,b){if(sortValue==="name")return a.name.localeCompare(b.name);if(sortValue==="level")return(b.level-a.level)||(b.xp-a.xp);if(sortValue==="progress")return(b.xp/b.xpRequired)-(a.xp/a.xpRequired);return new Date(b.lastUsedAt||0)-new Date(a.lastUsedAt||0)}
function openIconPicker(targetInputId){currentIconTarget=targetInputId;renderIconPicker("All");openModal("iconPickerModal")}
function renderIconPicker(category){$("#iconFilterBar").innerHTML=Object.keys(ICON_SETS).map(cat=>`<button class="filter-pill ${cat===category?"active":""}" data-icon-filter="${cat}">${cat}</button>`).join("");$("#iconGrid").innerHTML=ICON_SETS[category].map(icon=>`<button class="icon-btn" data-pick-icon="${icon}">${icon}</button>`).join("")}
function exportSave(){const blob=new Blob([JSON.stringify(state,null,2)],{type:"application/json"}),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download="skillforge-save.json";a.click();URL.revokeObjectURL(url)}
function importSave(file){const reader=new FileReader();reader.onload=()=>{try{const parsed=JSON.parse(reader.result);ensureDataShape(parsed);state=parsed;handleDailyReset(state);saveState();render();showToast("Save imported.","good")}catch(e){showToast("Import failed. Bad JSON.","bad")}};reader.readAsText(file)}
const escapeHtml=v=>String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#39;"),formatDate=v=>!v?"":new Date(v).toLocaleDateString(undefined,{month:"short",day:"numeric"});
document.addEventListener("click",e=>{const btn=e.target.closest("button, .picker-trigger");if(!btn)return;if(btn.matches("[data-tab]"))setActiveTab(btn.dataset.tab);if(btn.matches("[data-go-tab]"))setActiveTab(btn.dataset.goTab);if(btn.id==="quickAddBtn")$("#quickAddMenu").classList.toggle("hidden");if(btn.id==="settingsBtn")openModal("settingsModal");if(btn.id==="walletBtn")setActiveTab("rewards");if(btn.matches("[data-open-modal]")){const t=btn.dataset.openModal;if(t==="skillModal")openSkillModal();if(t==="habitModal")openHabitModal();if(t==="rewardModal")openRewardModal();if(t==="questModal")openQuestModal()}if(btn.matches("[data-close-modal]")||btn.id==="modalBackdrop")closeAllModals();if(btn.matches("[data-toggle-skill]")){const s=getSkill(btn.dataset.toggleSkill);if(s){s.expanded=!s.expanded;saveState();render()}}if(btn.matches("[data-edit-skill]"))openSkillModal(btn.dataset.editSkill);if(btn.matches("[data-edit-habit]"))openHabitModal(btn.dataset.editHabit);if(btn.matches("[data-edit-reward]"))openRewardModal(btn.dataset.editReward);if(btn.matches("[data-edit-quest]"))openQuestModal(btn.dataset.editQuest);if(btn.matches("[data-add-habit-under]"))openHabitModal(null,btn.dataset.addHabitUnder);if(btn.matches("[data-hit-habit]"))applyHabit(btn.dataset.hitHabit,btn);if(btn.matches("[data-undo-habit]"))undoHabit(btn.dataset.undoHabit,btn);if(btn.matches("[data-buy-reward]"))buyReward(btn.dataset.buyReward);if(btn.matches("[data-reward-filter]")){rewardFilter=btn.dataset.rewardFilter;renderRewards()}if(btn.matches("[data-quest-filter]")){questFilter=btn.dataset.questFilter;renderQuests()}if(btn.matches("[data-skill-focus]")){setActiveTab("skills");setTimeout(()=>document.getElementById(`skill-${btn.dataset.skillFocus}`)?.scrollIntoView({behavior:"smooth",block:"center"}),30)}if(btn.matches("[data-picker-target]"))openIconPicker(btn.dataset.pickerTarget);if(btn.matches("[data-icon-filter]"))renderIconPicker(btn.dataset.iconFilter);if(btn.matches("[data-pick-icon]")){if(currentIconTarget){const input=document.getElementById(currentIconTarget),preview=document.getElementById(`${currentIconTarget}Preview`);input.value=btn.dataset.pickIcon;if(preview)preview.textContent=btn.dataset.pickIcon}closeAllModals()}if(btn.id==="exportBtn")exportSave();if(btn.id==="resetBtn"){if(confirm("Reset everything? This wipes the app.")){localStorage.removeItem(STORAGE_KEY);state=defaultState();render();closeAllModals();showToast("App reset.","good")}}if(btn.id==="deleteSkillBtn")deleteSkill();if(btn.id==="deleteHabitBtn")deleteHabit();if(btn.id==="deleteRewardBtn")deleteReward();if(btn.id==="deleteQuestBtn")deleteQuest()});
document.addEventListener("input",e=>{if(e.target.id==="skillSearchInput"){searchValue=e.target.value;renderSkills()}if(e.target.id==="skillSortSelect"){sortValue=e.target.value;renderSkills()}});
$("#questType").addEventListener("change",updateQuestTargetVisibility);$("#skillForm").addEventListener("submit",submitSkillForm);$("#habitForm").addEventListener("submit",submitHabitForm);$("#rewardForm").addEventListener("submit",submitRewardForm);$("#questForm").addEventListener("submit",submitQuestForm);$("#importInput").addEventListener("change",e=>{const file=e.target.files?.[0];if(file)importSave(file)});
render();setActiveTab("dashboard");
